diff --git a/include/crow/app.h b/include/crow/app.h
index c4649d5..ae2b7ad 100644
--- a/include/crow/app.h
+++ b/include/crow/app.h
@@ -350,6 +350,18 @@ namespace crow
             return max_payload_;
         }
 
+        /// Set the maximum buffered HTTP request body size.
+        self_t& max_request_body(uint64_t maximum_body_size)
+        {
+            max_request_body_ = maximum_body_size;
+            return *this;
+        }
+
+        uint64_t max_request_body()
+        {
+            return max_request_body_;
+        }
+
         self_t& signal_clear()
         {
             signals_.clear();
@@ -887,6 +899,7 @@ namespace crow
         unsigned int concurrency_ = 2;
         std::atomic_bool is_bound_ = false;
         uint64_t max_payload_{UINT64_MAX};
+        uint64_t max_request_body_{UINT64_MAX};
         std::string server_name_ = std::string("Crow/") + VERSION;
         std::string bindaddr_ = "0.0.0.0";
         bool use_unix_ = false;
diff --git a/include/crow/http_connection.h b/include/crow/http_connection.h
index 20013db..2e4a9ac 100644
--- a/include/crow/http_connection.h
+++ b/include/crow/http_connection.h
@@ -110,6 +110,11 @@ namespace crow
             });
         }
 
+        uint64_t max_request_body() const
+        {
+            return handler_->max_request_body();
+        }
+
         void handle_url()
         {
             routing_handle_result_ = handler_->handle_initial(req_, res);
diff --git a/include/crow/parser.h b/include/crow/parser.h
index 8ca963a..b252be0 100644
--- a/include/crow/parser.h
+++ b/include/crow/parser.h
@@ -77,6 +77,10 @@ namespace crow
 
             self->set_connection_parameters();
 
+            if (self->content_length != UINT64_MAX && self->content_length > self->maximum_body_size_)
+            {
+                return 1;
+            }
             self->process_header();
             return 0;
         }
@@ -88,6 +92,11 @@ namespace crow
         static int on_body(http_parser* self_, const char* at, size_t length)
         {
             HTTPParser* self = static_cast<HTTPParser*>(self_);
+            if (self->req.body.size() > self->maximum_body_size_ ||
+                length > self->maximum_body_size_ - self->req.body.size())
+            {
+                return 1;
+            }
             self->req.body.insert(self->req.body.end(), at, at + length);
             return 0;
         }
@@ -103,7 +112,8 @@ namespace crow
         }
         HTTPParser(Handler* handler):
           http_parser(),
-          handler_(handler)
+          handler_(handler),
+          maximum_body_size_(handler->max_request_body())
         {
             http_parser_init(this);
         }
@@ -173,5 +183,6 @@ namespace crow
         std::string header_value;
 
         Handler* handler_; ///< This is currently an HTTP connection object (\ref crow.Connection).
+        uint64_t maximum_body_size_;
     };
 } // namespace crow
