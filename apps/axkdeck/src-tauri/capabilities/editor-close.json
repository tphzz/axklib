{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "editor-close",
  "description": "Close the main window after unsaved editor confirmation.",
  "windows": ["main"],
  "permissions": ["core:window:allow-destroy"]
}
